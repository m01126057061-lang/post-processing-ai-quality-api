[pytest]
asyncio_mode = auto
testpaths = tests
addopts = --tb=short -q
markers =
    unit: Unit tests (ML models mocked, no I/O)
    integration: Integration tests against the FastAPI TestClient
    benchmark: Performance benchmarks (run with --benchmark-only)
